package com.benlau.bofteam1;

import static org.junit.Assert.*;
import android.content.SharedPreferences;

import androidx.appcompat.app.AppCompatActivity;

import org.junit.Test;

public class ProfileIntegrationTest{

    //IN THE PROCESS OF WRITING TESTS
    /*
    private SharedPreferences preferences;

    @Test
    public void testSharedPreferenceAcrossJavaClasses() {
        //set a new value for the key "Profile" to ensure that preferences is accessible from other classes
        SharedPreferences.Editor editor = this.preferences.edit();
        editor.putString("testKey", "Rollin' around at the speed of sound");
        editor.apply();

        //assert that this new value is gettable in this java class
        //assertEquals("Testing Hello Equivalence", "hello", "hello");
        assertTrue("Rollin' around at the speed of sound".equals(testSharedPreferenceAcrossJavaClassesHelper()));
        editor.clear().commit();
    }


    //helper function for the above testSharedPreferenceAcrossJavaClasses()
    public String testSharedPreferenceAcrossJavaClassesHelper() {
        return preferences.getString("testKey", "");
    }
    */

}
